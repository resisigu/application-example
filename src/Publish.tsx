import React from 'react';
import { Frame } from '@lidojs/screen';
import { data } from '../data';

const Publish = () => {
    return <Frame width={1920} height={1080} data={data} />;
};

export default Publish;
